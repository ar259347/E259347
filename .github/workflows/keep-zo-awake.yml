name: Keep Zo Awake
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Zo Space health endpoint
        run: |
          curl -f https://arpi.zo.space/api/health
          echo "Ping successful at $(date)"