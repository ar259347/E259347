# GitHub Actions workflow (runs free)
# .github/workflows/keep-alive.yml
on:
  schedule:
    - cron: '*/5 * * * *'  # every 5 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS https://arpi.zo.space/api/health > /dev/null
